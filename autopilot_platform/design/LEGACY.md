# TestPilot design 遗留树已迁出包路径。
#
# AUD-2026-18：禁止接线；门禁 tests/test_archive_exclusion.py
#
# 物理位置：仓库根 archive/testpilot_design_legacy/
# 正式运行时请使用 autopilot_platform.platform（API / RAG / 生成）。
#
# 不要尝试修复 archive 内的 config/utils/langchain 引用——那是旧包布局，
# 不是漏移植。IDE 请将 archive 标为 Excluded（见该目录 README）。
